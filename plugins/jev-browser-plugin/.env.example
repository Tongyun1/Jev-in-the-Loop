# Copy these fields into ~/.config/jev-browser/config.env (outside the repository).
# Never commit a real key. Do not put the key in an issue or chat message.
TYPESAFE_API_KEY=
TYPESAFE_MODEL=jev-1.13.0

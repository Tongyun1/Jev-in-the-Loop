# 旋律续写采样记录（2026-09-22）

用 C major、120 BPM、四音用户动机，在浏览器中各运行约一分钟。每小节均向真实 Jev 接口发出一个包含十个并行问题的请求。采样时记录界面所显示的当前播放短句、下一小节计划、短句概率和最近八个音。

| 描述 | 已观察到的播放 / 计划片段 | 分布顶部 |
| --- | --- | --- |
| `雨夜，克制，慢慢变亮` | 第 25 小节：`A4 G4 D4 C4`；最近两小节为同一组 `A4 G4 D4 C4` | 动机回声 26%、弧线旋律 24%、移位模进 16% |
| `旷野，阳光，向远处生长` | 第 5 小节：`A4 C5 B4 E5`；第 19 小节又出现 `A4 C5 B4 E5`。第 12 小节：`C5 E5 G5 D5`；第 26 小节：`G5 B4 D5 A4` | 移位模进 26%、温柔回答 22%、动机回声 19%；另一轮弧线旋律 37%、悬念问句 21% |

这些数字不是完整乐曲评分，只说明当前候选池的实际行为。两个描述的顶部概率都分散在约 10%–37%；模型没有以高置信度锁死某个短句。相似感主要来自候选的底层音程和节奏计划，而不是 Jev 概率本身过于集中。

## 发现

1. `motifFromNotes()` 在用户弹出三个以上音后，始终只取这组用户音作为动机来源。每一小节都从同一组间隔重做 12 个候选，因此即使短句 ID 变化，也很容易回到相同的上行、回落和四音节奏。
2. 本地历史只保留四小节。短句在第五小节以后可以重新出现；当前重复惩罚只看 ID、完整音高序列和非常粗略的轮廓，未比较跨 4–8 小节的相似旋律。
3. Jev 返回的是分布，但客户端把它加权后仍取最高分候选。模型给出 20% 对 18% 这类很接近的判断时，播放端没有利用这种不确定性来选择不同但仍合理的备选。
4. 候选中大多数是四颗等量级音符；问答、倒影、模进虽然名称不同，但音符数量、落点和终止方式接近，听觉差异不足。

## 下一轮改造顺序

1. 保留原始用户动机作为主题 DNA，但在发展和转折小节从上一句系统旋律抽取变体；只在开头和回归处直接回声。
2. 保留八小节的短元数据（ID、节奏签名、音程签名、终止音、重音位置），不扩大原始音符上下文；在本地计算跨句相似度并对近似句强惩罚。
3. 将 Jev 的概率在通过新颖性过滤的前 3–4 项之间做受控抽样，而不是总取最高分；四小节句尾仍保留明确的收束规则。
4. 补足真正不同的节奏素材：两音长句、切分、上弱拍进入、三连 / 装饰、留白和跨小节延音。每种素材必须有不同的音程轮廓与落点。
5. 在旋律稳定后，把调内和弦写成带情绪说明的候选：I/IV 大三和弦偏明亮开阔，ii/iii/vi 小三和弦偏内省柔和，V 与 V7 负责期待和张力。旋律强拍优先落在当前和弦音上。

## 第二轮采样（旋律层改造后，同日）

用 `tools/sample.mjs`（与 app.js 每小节决策链一致、真实 Jev 接口）对四个描述各采样 24–40 小节。第一轮的四个问题全部改善或确认修复：音高级重复为 0；受控抽样稳定生效（模型首选被音乐性排序覆盖的比率约 63%–72%，均出现在分布接近时）；动机素材已在发展小节切换为系统旋律变体。

新的两个瓶颈（均已修复并复测）：

| 指标 | 雨夜 40 小节 | 旷野 40 小节 | 说明 |
| --- | --- | --- | --- |
| 音高完全重复（8 小节窗） | 0 | 0 | 旧问题已消除 |
| 节奏签名重复 | 16/40 | 13/40 | 同一节奏型隔几小节回流，是"相似感"新主因 |
| 实际进入 top3 的候选 | 6/12 | 6/12 | `breathing` 留白与 `ornament` 装饰从未被 Jev 排进前列 |
| 强拍音落在计划和弦音上 | 54% | 54% | 旋律与铺底各走各的，一半强拍悬在和弦外 |

修复内容（`static/melody.js`）：

1. 候选生成时把强拍（第 0/2 拍）音级吸附到本小节计划和弦（I→vi→IV→V）最近和弦音；弱拍仍自由经过。
2. 选择器新增节奏签名惩罚（每次相同节奏回流 −0.06），被埋没的留白、装饰与三音长句得以浮出。

复测（同描述各 32 小节）：节奏重复 16→3、13→9；强拍对齐 54%→79%、54%→83%；雨夜轮启用 11/12 个候选（`breathing` 与 `ornament` 首次自然出现）。四个描述的短语分布各不相同（风暴偏 `leap/cadence`，雾中偏 `question/inversion`），说明描述词已实质影响旋律走向。

## 遗留观察

- `leap`（跳进）在旷野/风暴类描述里出现频率仍偏高，后续可按描述情绪对跳进加权。
- 和声铺底目前几乎全在 open 排列；可让 Jev 的 voicing 选择更受前和弦连接成本影响。
- 下一步素材方向：五音句、附点+切分组合、跨小节延音、四小节动机的完整回归（第 16 小节整句重现）。

## 第三轮升级：节奏库与调性系统（同日晚）

针对"节奏单调（全是八分均分）"与"悲伤应转小调、键盘只有白键"两项反馈的改造与验证。

### 节奏库（melody.js）

- 新增 `RHYTHM_FAMILIES`：均分八分 / 附点推进 / 切分错位 / 十六分点缀四族节奏型，每两小节轮换族、族内轮换具体网格；12 个候选的 offsets 全部从当前族派生（个别候选保留固定特征节奏，如 breathing 长音、rhythmic_shift 切分）。
- 最短时值从 1/4 拍降到 1/8 拍（16 分音符可用），候选 intent 文案标注本小节节奏族。
- 采样验证：28 小节内四族分布 8/8/6/6，节奏签名 8 小节窗重复 0。

### 调性系统（melody.js / server.py / app.js / harmony.js）

- 音阶从 4 个扩到 24 个（12 大调 + 12 小调，主音折进共用音域）；新增 `registerBounds` 绝对音域保护（55–79），`voiceLeadChord` 改为跳过越界排列而不是 clamp（clamp 会破坏和弦结构——在 C# 大调上被测试抓到）。
- Jev 新增两问：`chromatic`（黑键浓度 0–3，明亮语境应接近 0）、`key_shift`（stay/relative/parallel/subdominant/dominant，五度圈转调）。问题数 10 → 12。
- 前端：调式下拉 24 项；虚拟钢琴升级为八度内 12 键（A–K 白键 + W E T Y U 黑键，黑键深色样式）；`chromaticAppetite` 平滑跟随 Jev 读数，弱拍半音邻音按 `appetite × 伪随机` 概率注入（小调减半）；`key_shift` 在四小节边界应用并即时重建钢琴与提示。
- **本地情绪门控**：真实采样发现 Jev 在完整上下文下对转调过于保守（悲伤描述 28 小节全 stay，最小上下文却敢给 parallel）。故增加规则：悲伤意象连续 3 小节仍处大调（或明亮意象连续 3 小节仍处小调）且 Jev 无显式转调时，本地排队 `relative` 转换（音集不变、动机完整保留）；Jev 显式 key_shift 优先；本地队列在情绪不再矛盾时自动撤销（"stay"不再清空本地队列——这是第三轮修掉的关键 bug）。

### 第三轮采样结论（各 28 小节，真实 Jev）

| 指标 | 悲伤·黄昏（思念/忧伤） | 欢快·晴空（骑行/追逐） |
| --- | --- | --- |
| 调性旅程 | C major → A minor（bar9 起转关系小调） | 全程 C major |
| 小调小节占比 | 20/28 | 0/28 |
| 黑键（半音邻音）数量 | 33/104 | 4/103 |
| 节奏族分布 | even 8 · dotted 8 · sync 6 · 16th 6 | 同左 |
| 音高/节奏签名重复 | 0 / 0 | 0 / 0 |

悲伤语境自动转入小调且黑键更多、欢快语境保持大调且黑键接近零——两条需求的行为均由数据确认。

## 第四轮：调内约束与可听节奏（同日晚）

针对后续实际试听提出的“调外半音突兀”问题，自动旋律已取消 `chromatic` 插音和对应 JEV 问题。黑键现在只作为当前调内的升降音出现，例如 C 小调的 Eb、Ab、Bb。键盘只给当前七音阶加高八度主音，共八个可选音。旧的第三轮“黑键数量”记录保留作为历史采样，已经不代表现行系统。

测试发现部分小调主音 MIDI 与下拉名称不符，现已修正 24 个调的主音音高，并增加所有关系大小调音集一致的检查。转调边界的旋律与和弦也提前按目标调生成，避免边界播放旧调候选。

节奏延音改为按相邻音间距和句式计算，留白句可持续两拍以上；切分句更短，强拍与弱拍有不同力度。浏览器实测 C 小调的旋律与和弦均显示调内音，并能看到当前正在播放的音与时值。

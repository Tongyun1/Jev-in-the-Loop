# JEV 决策设计：一小节旋律、调内和声与一个活球体

## 角色分工

Jev 判断哪条已经可播放的旋律及哪一个调内和弦最符合故事、动机与曲式；本地的 `melody.js` 与 `harmony.js` 负责生成候选、保持调式与音区、调整八度和排除重复。Jev 是选择器，候选质量直接限制音乐质量。下一步若要写更长且完全原创的主题，可加入专门的作曲模型生成候选，但仍让 Jev 做实时选择。

## 每次请求的 state

输入只保留最近四小节、最多 32 个音符事件，格式如下：

```text
recent_events: [相对小节, MIDI 音高, 十六分音符位置, 十六分时值, 力度百分比, u/s]
phrase_history: 最近八个已选短句 ID
creative_brief: 用户完整文字描述
current_image: 描述中当前推进到的意境片段
melody_rules: 当前调式、可用音集、音区、十六分网格、和声功能、四小节曲式阶段
motif: 最近音级与上行/下行轮廓
harmonic_history: 最近四个实际播放的和弦 ID
```

例如用户写 `雨夜，克制，慢慢变亮`，完整文字每次都随 state 传递；`current_image` 在八小节内从雨夜逐步走向变亮。

12 个候选各有 `id`、简短音乐意图和一次编码的实际播放计划：

```text
plan: [音级, 十六分音符位置, 十六分时值, 力度百分比]
```

客户端把 Jev 判断的**同一份候选对象**保存到下一小节再播放。它不会在播放前重新计算音符。

和声候选受当前调式约束，包含根音级数、triad / add9 / diatonic seventh 色彩、罗马数字和在最近小节是否已使用。首版只采用自然音阶内 I、vi、IV、V 等功能，并按 I–vi–IV–V 的四小节倾向生成；Jev 只选择候选和基础配音，客户端执行原位、第一转位、第二转位或开放式，并相对上一个和弦选择最小音程移动。

## 一个请求内的十一个问题

| 问题 | Primitive | 作用 |
| --- | --- | --- |
| `phrase` | Choice | 从 12 条具体旋律中选下一小节 |
| `harmony` | Choice | 从当前调式的和弦候选中选铺底功能与色彩 |
| `voicing` | Choice | 选原位、第一转位、第二转位或开放式 |
| `density` | Score | 判断下一小节需要多少音符 |
| `key_shift` | Choice | 决定下一个四小节段是否转向关系、同主音或五度邻近调 |
| `motif_keep` | Noul | 判断动机是否应该被清楚保留 |
| `visual_mood` | Choice | 选蓝、琥珀、红、紫或金色主情绪 |
| `visual_scene` | Choice | 选球体表面的流光、深潮、火花、棱镜或盛放形变 |
| `visual_temperature` | Score | 冷暖偏移 |
| `visual_motion` | Score | 运动幅度 |
| `visual_luminance` | Score | 发光程度 |

`phrase` 指令要求 Jev 依照完整文字与当前意境、用户动机、四小节曲式、和声与节奏来选。它必须避免机械级进与最近的重复短句，并考虑这一小节如何引出下一句。音乐选择的本地排序保留 Jev 概率，同时给近期同一短句、同轮廓、同音符计划及同一音高序列减分；在第四小节鼓励收束。`density` 与 `motif_keep` 也进入排序。

调式由七个音级确定。键盘显示该音阶七音加高八度主音；升降音只有属于当前调时才出现。Jev 不直接生成任意 MIDI 音高，也不再通过“黑键浓度”插入调外半音。排队的转调在四小节边界生效，而边界小节的旋律与和弦候选提前按目标调生成。

色彩首先采用 Jev 的判断；若当前意境片段有明确的颜色情绪词，本地映射会约束主色，让“雨夜”的开头保持冷色、“慢慢变亮”到来后再转向金色。球体形态、运动和亮度仍使用同次 Jev 响应。

## 频率和边界

启动时规划两小节，之后每两小节规划一次。每轮串行调用 `/plan` 与 `/decision` 两次；4/4 的平均频率是 `BPM / 240` 次每秒，120 BPM 即平均每 2 秒一次调用，但两次调用集中在每 4 秒的规划边界。若上一请求仍未返回，保留已选且可播放的短句，避免并发堆积。服务端短时超时会显式显示回退来源。`context_chars` 是请求 JSON 字符数，不代表模型 token 用量。

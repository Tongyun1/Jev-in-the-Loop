# 两小节节奏与旋律规划

## 为什么先规划时槽

Jev 是有类型的快速判断器，不直接输出任意长度的音符序列。旧版 12 条候选大多只有 3–4 个起音；即使 Jev 改选候选，听感仍被候选库限制。现在先决定每小节的起音数量与节奏族，再由本地乐理规则生成具体音高、休止与时值，最后让 Jev 在完整的两小节乐句里选择。

## 每轮决策

1. `/plan`：四个独立 Choice，分别选第一、第二小节的起音数（2–8）与节奏族（均分、附点、切分、十六分）。输入包括当前意境、能量、最近四小节的压缩音符与用户动机；不传全部演奏史。
2. 本地候选生成：在 4/4 的十六分网格上放置起音，间隔决定休止和延音；动机的音程经回声、倒影、逆行、问答等变奏形成 12 条两小节候选。所有音高映射到当前自然调式并限制音域。少数「呼吸」候选会减少起音，少数「装饰」候选会增加起音，使 Jev 有真正的密度对照。
3. `/decision`：Jev 在完整播放计划中选一条乐句，同时选两小节的调内和弦、转位、视觉状态。原播放计划缓存到边界直接演奏；不在播放前重新生成。
4. 客户端每阶段设置 1.75 秒时限。在 120 BPM、每两小节 4 秒的窗口内，超时会明确转为本地规则。服务器断线仍可播放。

**为何不逐音调用：**两个小节可能有 4–16 个起音，逐音请求需要许多次网络往返，难以稳定在音乐时钟前做完，也容易只顾下一音而没有句尾。一次规划和一次整句评判，让节奏与旋律分别有明确职责。

## 后续试听指标

对「雨夜，克制」「温暖，慢慢变亮」「激昂推进」各运行 1–2 分钟，记录每小节起音数、不同节奏族占比、音高重复率、完全相同的两小节指纹、强拍与和弦音的对齐率，并人工试听句尾是否有呼吸。`tools/sample.mjs` 仍是旧版单小节采样链，不能作为本版的效果证据。
